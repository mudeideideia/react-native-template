import styled from 'styled-components/native'

const StyledComponents = { }

StyledComponents.StyledView = styled.View`
  background-color: #ddd;
`

StyledComponents.StyledText = styled.Text`
  color: #000;
`

export default StyledComponents